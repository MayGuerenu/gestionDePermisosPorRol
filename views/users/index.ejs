<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Usuarios</title>
</head>
<body>
  <h1>Listado de Usuarios</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><strong><%= user.name %></strong> — <%= user.email %></li>
    <% }) %>
  </ul>
</body>
</html>
